project.ext {
    submodules = ['database', 'auth', 'storage']
    group = 'io.stepuplabs.firebaseui.android'
    version = '1.2.0.6'
    pomdesc = 'Firebase UI Android'

    compileSdk = 25
    targetSdk = 25
    minSdk = 14

    buildTools = '25.0.2'

    firebaseVersion = '11.0.0'
    supportLibraryVersion = '25.3.1'
}
